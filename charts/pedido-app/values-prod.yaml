backend:
  replicas: 2
  hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 5
  image:
    tag: "1.0.0"

frontend:
  replicas: 2
  image:
    tag: "1.0.0"

ingress:
  host: "prod.pedidos.local"